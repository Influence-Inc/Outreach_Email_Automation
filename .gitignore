node_modules/
.env
*.log
.DS_Store

# Reply-bank: harvested examples contain creator PII (real inbound text,
# emails, thread ids). The seed file is committed; harvested output is not.
backend/data/harvested_examples.json
